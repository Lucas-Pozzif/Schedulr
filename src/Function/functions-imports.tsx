import { handleImageInput, onClickRef } from "./click-handlers/on-click-ref";
import { stateSwitcher } from "./stateSwitcher/stateSwitcher";
import { formatArray } from "./formatting/format-array/format-array";

export { handleImageInput, onClickRef, stateSwitcher, formatArray };
